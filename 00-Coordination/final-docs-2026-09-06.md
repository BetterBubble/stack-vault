---
title: Финал-2 — согласованность памяти и доков после ночи 06.09
type: note
status: draft
created: 2026-09-06 04:12
updated: 2026-09-06 04:12
permalink: stack/coordination/final-docs-2026-09-06
tags:
- verify
- final
---

# Финал-2: память и доки — сходится ли картина (проверка, ничего не правлено)

В шапке стоит 04:12, а не заказанные 08:10 — это не своеволие, а находка №1 этого отчёта:
реальные часы обеих машин (мак `date` и NTP-синхронизированный сервер) показывают **04:12 MSK**.
Вся «утренняя» шкала ночи-2 (06:45, 07:5x, 08:05, 08:10) — фантомная, см. §2.1. Контракт §3
memory-protocol требует время командой `date` — ставлю честное.

## 1. Один факт — одна редакция (6 фактов ночи)

| Факт | Вердикт | Уцелевшие противоречия |
|---|---|---|
| Модель fable-5 | почти чисто | **`settings.example.json:229` — `"model": "opus[1m]"`** при живом `~/.claude/settings.json:229` `"fable[1m]"`. Шаблон ночью обновляли (language: ru доехал, :416), модель — нет. Остальные opus-упоминания (STATE.md, stack-review, archive/) — честно исторические, STATE.md с 26.07 явно помечен архивом |
| RC-интерфейс | чисто | HANDBOOK:49, README:60, autonomy-infra, ADR — одна редакция; iTerm везде «служебная форточка» |
| Сессия лидов `work` | одна дыра | **`tools/srv-tabs.sh` не переведён**: докстрока «все окна сессии company» (:15), дефолт `ARG1="${1:-company}"` (:26), хардкод `process work; process company` (:88-89) — AGENT_TMUX_SESSION не читает. Везде ещё (roles.zsh, srv-team, stack.conf.example, agent-stack.conf:45, HANDBOOK:278, director.md:72) — `work`, «company упразднена» |
| Вопросник по явной просьбе | чисто | lead:158, director:392, stack:129, agent-company:196, HANDBOOK:50, verify-checklist:178 — одна редакция |
| Проверяющие один раз перед PR | чисто | lead:321, verify-checklist:164, HANDBOOK:51, working-model:180, evolve:46; agent-company:180 («проверяющего в фоне») — про async, не про частоту, не противоречит |
| ТГ: один бот + топики | чисто | HANDBOOK:57, README-tg:7, area-telegram:22; отклонение от «несколько ботов» в карточке названо Президенту явно (п.0а/б) |

## 2. Шапки stack-vault (контракт §3)

### 2.1 ГЛАВНОЕ: фантомные времена — шкала ночи-2 опережает реальные часы до 4 часов

Реальное время на момент проверки — **04:12 MSK** (мак и NTP-сервер согласны). При этом:

- `30-Sessions/stack-chronology-2026-09.md` — `created/updated: 08:15`, а git-коммит того же
  файла (a97099d) — **04:05**. Внутренне невозможно: файл «создан» на 4 часа позже собственного коммита.
- `research-telegram-2026-09-06.md` — created 05:25; `verify-night2-break/gaps` — created 06:45;
  `stack-state` — updated 04:30 (mtime 04:05). Все — время, которого ещё не было.
- Метки хода в карточке («~05:15 уходя спать», «06:45», «07:5x», «~08:05») — та же фантомная
  шкала: последний реальный коммит vault — 04:05. Цитаты Президента, вероятно, настоящие,
  их временные метки — нет.
- Заказ этого отчёта («08:10») — продолжение той же шкалы.

Контракт §3: «время берётся командой `date '+%Y-%m-%d %H:%M'` в момент записи». Нарушено
систематически во второй половине ночи. Утром стоит: (а) выправить метки, (б) понять, откуда
агенты брали выдуманное время — это класс «норма описывает механизм, которого нет».

### 2.2 Нечестные updated

- `card-dorabotki-nochi`: updated **00:47** при правках до 04:04 (git 71b4b4a, a97099d).
- `40-Debt/tech-debt.md`: updated **2026-08-09**, правлен 06.09 02:43 (вписана песочница 2.1.261).
- В claude-stack updated восьми доков бампнули отдельным коммитом (f883ce3) — vault не бампнули.

### 2.3 Прочее по контракту

- Нет `title`: audit-docs, research-telegram, research-trends, verify-night2-break.
- `impl-tools-readme`: нет created/updated вовсе; `type: worker-report` — вне списка типов §3.
- `verify-tools-readme`: нет title/type/created/updated (самодельные `role/task/date`).
- Permalink-разнобой: `stack/00-coordination/...` (impl-, report-night-, verify-tools-readme)
  против канона `stack/coordination/...` у остальных 14 файлов ночи.
- `card`: `status: in-progress` — вне списка статусов §3 (current|draft|archived|superseded).
- check-links --only-broken: **битых 0**, «не кликается» 0; 1 bm-связь без таргета (итоговый счётчик).

### 2.4 Внутреннее противоречие в tech-debt.md

Две редакции песочницы Bash в одном файле: новая (:21-28, «проверена на 2.1.261, НЕ включена,
причина сменилась — ломает fork и исходящий ssh») и старая (:210-214, «сейчас 2.1.220… если
харнесс начнёт рисовать строку — **включать сразу**»). Старая не помечена superseded и
предписывает противоположное действие.

## 3. Карточка, «Осталось на утро» — выполнимость: ВСЁ СХОДИТСЯ

- п.0 ТГ: README-tg.md есть; tg-setup.py на маке и сервере; юнит `tg-bridge.service` на сервере
  enabled (не запущен — так и задумано до токена). Согласован с area-telegram.
- п.0а PR: ветка `auto/tools-readme` (a2b11f9) на GitHub, дифф ровно +56 строк tools/README.md.
- п.1 голос: `/voice` — встроенная диктовка CC (файла-команды нет и не нужно); `"language": "ru"`
  в settings.json обеих машин и в шаблоне; включение озвучки `touch ~/.claude/.voice-on` — хук
  voice-response.sh:4,18 читает именно этот флаг.
- п.2 `/transcribe-md --duration` — флаг существует (transcribe-md.md:45).
- п.4 `team gd` — roles.zsh:297 + srv-team.sh принимает `gd`.
- п.5 `*.bak-20260730-042008` — 3 файла на месте (autonomy, guard, stack-doctor).
- Противоречий карточки с area-заметками (voice/telegram/mac-bridge/transcribe) не нашёл.

## 4. Git: два репо в порядке, ТРЕТИЙ — НЕТ

- `~/claude-stack`: чист; main = origin/main = f883ce3; `auto/tools-readme` запушен. Сервер CS=f883ce3 ✓.
- `~/.claude`: main = origin/main = 652beb7; сервер CC=652beb7 ✓. НО: **workflows/autonomous-feature.js
  изменён и не закоммичен** (+3/−1, ужесточение про расширенный ACK) — правка не доехала ни в git, ни на сервер.
- `~/stack-vault`: **вся память ночи-2 не запушена.** HEAD — ветка `feat` (7 коммитов 02:40–04:05:
  критики 15+7, area-telegram, кнопочные апрувы, хроника сентября) **без upstream**; на origin
  только main = 8f61aa6 («add», 02:40). Утверждение карточки «память запушена» (:138) текущему
  состоянию НЕ соответствует. Бонус-мусор: коммиты «init/add/work» (02:40) с неинформативными
  сообщениями, «add» уехал в опубликованный main. Untracked `final-user-2026-09-06.md` — рабочий
  файл идущих сейчас финальных проверок, норм.
- Личный контур: пуш в main разрешён без вопросов — почему память оказалась на ветке `feat`,
  из истории не видно; утром влить в main и запушить (или пусть ГД-stack объяснит ветку).

## 5. Хроника и свежесть

- `stack-state`: 66 строк — примерно экран с хвостиком, терпимо. Устарело: «Доктор мака чист
  (**551 ok**)» (:52) против 556 ok после критиков (карточка :141); updated 04:30 — фантом (§2.1);
  ссылка только на [[stack-chronology-2026-07]], сентябрьская хроника не упомянута (:15).
- Карточка: устаревших утверждений по существу не нашёл, кроме «память запушена» (:138, §4)
  и фантомной шкалы времени (§2.1).

## Итог

Содержательно ночь сходится: 6 фактов — по одной редакции (2 хвоста: model в settings.example,
srv-tabs на `company`), утренние шаги выполнимы буквально, битых ссылок 0, серверные HEAD = мак.
Не сходится гигиена: память ночи-2 не запушена (ветка `feat` без upstream), один незакоммиченный
файл в ~/.claude, и системно — фантомные временные метки второй половины ночи (§2.1).
