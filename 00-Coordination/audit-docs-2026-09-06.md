---
type: note
status: draft
created: 2026-09-06 02:40
updated: 2026-09-06 02:40
permalink: stack/coordination/audit-docs-2026-09-06
tags:
- audit
- docs
---

# Аудит согласованности доков стека — 06.09.2026

Скоуп: `~/claude-stack` (HANDBOOK, working-model, agent-company, signals, session-lifecycle,
verify-checklist, roles/, README) + `~/.claude/commands/*.md`. Искомый класс дефекта: одно правило
в двух редакциях, которые разошлись; плюс отставание доков от изменений 06.09.

**Оговорка по строкам:** номера — по состоянию на коммит `4581530`; доки правились параллельно
аудиту, строки могли сдвинуться. Цитаты точные, ищутся грепом.

Что проверено и **сошлось** (перечисляю, чтобы не перепроверять): сигнал ГД только веха/блокер и
тело ≤3 строк (signals.md:103 · lead.md:116–120, 436 · director.md:107, 173); сводки ГД ≤10 строк
(director.md:70); онбординг-доклад лида ровно одной строкой (lead.md:102); вопросник только по
явной просьбе (director.md:391 · lead.md:158 · stack.md:129 · verify-checklist.md:178 · HANDBOOK:50);
проверяющие — один заход перед пушем/PR (HANDBOOK:51 · lead.md:310 · verify-checklist.md:164 ·
agent-company.md:112 «проверка идёт ОДИН раз» · commands/evolve.md:46); монитор не вооружается
(signals.md:150+ · verify-checklist.md:53 · обе персоны, ритуал п.0); ГД 95% и чекпойнт по пакетам
(director.md:96–116); slash-команды в `~/.claude/commands/` чисты от устаревших ссылок.

---

## А. Правило в двух редакциях (главный класс)

### А1. Модель ролей — Opus против fable-5, три места + код

1. **HANDBOOK.md:341** · «Все роли на **Opus 5 с контекстом 1M**» · расходится с HANDBOOK.md:52
   («все роли и воркеры — **fable-5[1m]**») — две редакции внутри одного файла · Правка: заменить
   на fable-5[1m], эффорты оставить.
2. **HANDBOOK.md:457, 463** · пример статусной строки «`Opus 5 1M·high`» · то же · Правка: обновить
   пример на fable-5.
3. **agent-company.md:54** · «**Модель всех ролей/агентов/субагентов — Opus** (H5)» · расходится с
   решением 06.09 (fable-5[1m]; lead.md:370 уже знает) · Правка: «— Fable 5 [1m] (H5, обновлено 06.09)».
   ⚠️ файл неприкосновенный — правка через `pending-apply/`.
4. **roles.zsh:253 и :287 (код, та же природа)** · `--model "${AGENT_MODEL:-opus[1m]}"` в `director()`
   и `stack()` при глобальном `_AGENT_MODEL="claude-fable-5[1m]"` (roles.zsh:52) — **ГД и stack
   реально стартуют на Opus**, остальные роли на fable-5. Комментарий в самом файле (:245–246)
   предупреждал: «разъедутся два места…» · Правка: подставить `$_AGENT_MODEL` вместо литералов.

### А2. Сессии tmux: company упразднена, но живёт в трёх местах

5. **HANDBOOK.md:273** · «Роли там живут окнами tmux: `company` — лиды, `work` — ГД и служебные
   окна» · расходится с stack.conf.example:7 (`AGENT_TMUX_SESSION=work` — «ОДНО место правды…
   введено 06.09 после раскола company/work») и open-lead.sh:210–216 · Правка: «окна живут в
   сессии из `AGENT_TMUX_SESSION` (сейчас `work`)».
6. **roles/director.md:71–74** · «…попали в сессию `work` … вместо `company`, где их ищет раскладка
   окон на маке» — обоснование красного правила теперь утверждает противоположное текущему порядку
   (лиды живут именно в `work`; раскладка мака при RC — не главный интерфейс) · Правка: переписать
   обоснование под «скрипт кладёт окно в `AGENT_TMUX_SESSION`, руками — мимо неё».
7. **roles.zsh:308, 313, 315 (код, та же природа)** · мак-`team()` ищет окна перебором `company work`
   с комментарием «окна лидов живут в company» · расходится с srv-team.sh (TSESS из
   `AGENT_TMUX_SESSION`) · Правка: читать ту же переменную, комментарий поправить.
8. **HANDBOOK.md:211** · «`open-lead qa` # новая вкладка iTerm2 + lead-qa в ней» · расходится с
   RC-порядком (окно уходит в tmux сервера; iTerm — «служебная форточка», HANDBOOK:49) · Правка:
   обновить комментарий примера.

### А3. AskUserQuestion — старая мягкая редакция уцелела

9. **agent-company.md:196 (F2)** · «`AskUserQuestion` — только на РЕАЛЬНЫЕ развилки, не лавиной» ·
   расходится с правилом 06.09 «только когда Президент сам попросил варианты» (5 мест, см. шапку) ·
   Правка: привести F2 к новой формулировке. ⚠️ неприкосновенный файл — `pending-apply/`.

### А4. Monitor для блокеров — противоречие внутри одного файла

10. **verify-checklist.md:176** · «Не busy-поллит блокеры — использует `Monitor`/`ScheduleWakeup`» ·
    расходится с тем же файлом :53 («`Monitor` НЕ вооружается (с 23.08)») и agent-company E1
    (серверный детач + сигнал/пуш) · Правка: «…использует серверный детач (`nohup`+маркер) и
    сигнал/пуш».

### А5. /checkpoint — два противоположных факта

11. **session-lifecycle.md:38** · «Президент не вызывал команду ни разу за месяц, и система
    подстроена под этот факт» · расходится с HANDBOOK.md:127–128 («Живёт одно: `/checkpoint` —
    14 исполнений за 10 дней, растёт») · Правка: обновить session-lifecycle на текущий факт.

---

## Б. Доки отстали от 06.09

12. **HANDBOOK.md:355** · «Семь команд. Шесть из семи…» · в таблице после добавления `/evolve` строк
    больше (счёт с /transcribe-md — десять) · Правка: убрать инвентарное число — сам HANDBOOK дважды
    объясняет, почему числа в прозе «протухают и начинают врать».
13. **HANDBOOK.md:174–176** · «ГД я открываю редко — 4 раза против 19 у лидов… опираться на эти
    напоминания нельзя» · рамка «Президент живёт в окнах лидов» перевёрнута правилом 06.09
    «ГД — единственная точка входа, 95%» (director.md:96) · Правка: пометить статистику как
    историю до 06.09 и переписать вывод.
14. **HANDBOOK, §1-таблица (:47–56)** · нет строки про «ГД — единственная точка входа (95%),
    чекпойнт по пакетам» — правило живёт только в персоне ГД · Правка: добавить строку в таблицу.
15. **HANDBOOK.md:966–983 (§19 Телефон-контроль)** · «Чтобы мозг переживал закрытие окна —
    `director-persist` (tmux)… `tmux -CC attach -t tacticum-brain`» · описывает мак-эпоху; при
    RC-порядке все сессии и так в tmux на сервере с автостартом · Правка: сверить §19 с §1,
    решить судьбу `director-persist`.
16. **HANDBOOK.md:1002–1004 (§20)** · «постоянного наблюдения нет… нужен либо persistent-монитор,
    либо внешний планировщик» · watchdog каждые 5 мин (HANDBOOK:53) — ровно внешний планировщик;
    тех-долг отстал · Правка: сузить долг до того, что watchdog не покрывает (признаки stuck-check).
17. **HANDBOOK, §18 таблица хуков (:940–958)** · нет `voice-response.sh` (Stop-хук,
    settings.json:320, появился 06.09) · Правка: добавить строку (низкий приоритет — таблица сама
    отсылает к команде-генератору списка).
18. **signals.md** · заявляет «единственное описание этого кода и обязан с ним совпадать» (:18),
    но не знает механизм принятых разрывов `~/.claude/.signal-gaps-accepted`
    (stack-doctor.sh:420–429, 06.09) · Правка: абзац в раздел «Обслуживание: .seq и seen/».
19. **session-lifecycle.md, триггеры (:37–40)** · нет триггера ГД «закрыл пакет → /checkpoint»
    (director.md:111, правило 06.09) · Правка: добавить строку-триггер.

### README.md — самый отставший файл скоупа (updated 26.07)

20. **README.md:132** · «Что у меня есть / какие команды → `company` (шпаргалка) + этот README» ·
    расходится с CLAUDE.md («Полная карта — HANDBOOK.md… Одна карта, а не три») · Правка: отсылать
    к HANDBOOK.
21. **README.md:58** · список команд roles.zsh без `open-lead`/`team`, ничего про RC-интерфейс,
    watchdog, мост, `/evolve` · Правка: дополнить одной строкой или заменить ссылкой на HANDBOOK §4.
22. **README.md:36–48 и раздел переноса (:103–125)** · карта частей и чеклист переноса не знают
    ни `tools/` (mac-bridge, mac-get, watchdog, srv-team…), ни машинный оверлей
    `~/.claude/settings.machine.json` (мержится доктором, stack-doctor.sh:568) · Правка: строка в
    таблицу частей + пункт в чеклист переноса.

---

## В. Мелочь / на подтверждение

23. **roles/stack.md:29** · «доложи состояние одним экраном» · возможно расходится с духом 06.09
    «онбординг-доклад одной строкой» (у лида — lead.md:102). Может быть намеренным (контур стека —
    прямой разговор), подтвердить у Президента или оставить с пометкой.
24. **working-model.md:179** · «Гейт после каждого содержательного изменения» (контур стека) ·
    читается как противоречие «проверка один раз» — стоит уточнить, что здесь «гейт» = механическая
    проверка (доктор/тесты), а не проверяющие субагенты.
25. **Гигиена шапок** · `updated:` в HANDBOOK, signals, verify-checklist, director, lead остался
    `2026-08-09`, хотя файлы правлены 06.09 (git) · Правка: обновлять `updated:` при правке.
