---
title: Отчёт автоном-прогона за ночь 05→06.08 — одна задача, встала на гейте
type: note
permalink: stack/board/report-night-2026-08-06
status: draft
created: 2026-08-06 00:22
updated: 2026-08-06 00:50
project: стек / автономка
tags:
- board
- autonomy
- report
---

# Отчёт автоном-прогона за ночь 05→06.08

**Одной строкой: прогон ночной (`nightShift: true`), статус `blocked`. Взята одна задача —
`readme-pending-apply`; правка сделана, гигиена диффа чистая, но гейт не пройден со второй попытки,
потому что порог приёмки в плане недостижим в объявленном скоупе. Мержить нечего, руками ничего не
горит.**

Ранняя версия этой заметки (00:22) фиксировала прогон как несостоявшийся — данные ниже её отменяют:
прогон состоялся и дошёл до гейта.

## 1. Сначала руками

**Руками ничего не горит.** Поля `needsHumanFirst` в данных нет: деплоя в прогоне не было, значит нет
ни деплоя без подтверждённого артефакта отката, ни деплоя, оборванного сбоем инструмента с
НЕИЗВЕСТНЫМ состоянием прода, ни не снятой точки отката.

## 2. Порядок мержа

**Мержить нечего.** Поля `mergeOrder` в данных нет: единственная ветка гейт не прошла, PR по ней не
создавался, стека веток не возникло — значит и порядка «снизу вверх» в эту ночь не существует.

## 3. Ветки, которые не запушены

Поле `pushed` в данных не выставлено ни для одной ветки: доставка ветки `auto/readme-pending-apply`
(база `main`) в данных не подтверждена. Гейт она не прошла, поэтому в доставку и не шла. Если решишь
доставить её руками:

```
git -C /Users/bubblemac/tacticum-worktrees/claude-stack-readme-pending-apply push origin auto/readme-pending-apply
```

## 4. Что сделано

**`readme-pending-apply`** — ветка `auto/readme-pending-apply`, база `main`, попыток 2, статус
`blocked-gate`.

- Изменён **1 файл** (`README.md`), **2 коммита**, рабочее дерево чистое (незакоммиченного 0).
- Гигиена диффа — всё чисто: нет файлов-секретов, нет мусора, нет похожего на секрет в добавленных
  строках, нет AI-подписей в коммитах, ветка не `main`/`master`, изменения в пределах скоупа плана
  (1 файл, все в скоупе).
- Метрики: `izmenennyh_faylov` — **1 против порога 1**, пройдено. `bityh_ssylok` — **3 против порога
  0**, провалено; при этом регрессии нет: baseline 3/3 → HEAD 3/3, вклад правки **0 новых** `[[…]]`.
- Второй прогон гейта (тот же вызов + `--plan
  /Users/bubblemac/stack-vault/00-Board/plan-dry-run-pending-apply-2026-08-06.md`) закрыл пункт
  «апрув плана»: строка «апрув: ГД, 06.08» и раздел «Что НЕ трогаем» на месте.
- По существу: README теперь предупреждает о лжи каталога `pending-apply`.

**Деплоя не было** — соответственно нет ни даты, ни времени, ни пути к артефакту отката.

## 5. Заблокированное и пропущенное

| Что | Причина (дословно из гейта) | Что нужно от человека |
|---|---|---|
| Гейт задачи, `ok_overall: false` | «Порог приёмки провален механически: `bityh_ssylok` = 3 против порога <= 0» (метрика взята из отчёта verifier дословно, проверена контролёром независимо) | см. строку ниже — кривой сам порог |
| Порог `bityh_ssylok <= 0` | «НЕДОСТИЖИМ в объявленном скоупе `["README.md"]`: все три битые ссылки лежат вне скоупа» — `[[research-orchestration-i-pamyat]]` в `session-lifecycle.md`, `[[…]]` в `roles/director.md`, `[[adr-perenos-na-vps-ovh]]` в `pending-apply/allow-rules-2026-08-05.md`. «Это дефект формулировки приёмки в плане, а не дефект правки» | Решение лида/планировщика: расширить скоуп на два-три чужих файла ЛИБО переписать порог как вклад-метрику («новых битых <= 0»). Контролёр это решение не принимает |
| Незакрытый пункт «гейт приёмки материалов» (`blocking_unchecked`) | «досье не передано (`--dossier`) — „тот ли репозиторий“ и „то ли сказано“ механически НЕ проверены; для мержа и деплоя это обязательный вход». Досье для этой задачи не существует вовсе — работа над стеком по прямому заказу, внешних материалов нет, но механически это НЕ проверено | Решить, как этот пункт закрывается для задач без досье |
| Замысел плана исполнен наполовину | Цель — «каталог `pending-apply` отвечает на вопрос, что ещё ждёт меня». Три применённых файла (`apply-2026-07-30.sh`, `allow-rules-2026-08-05.md`, `claude-md-window-cut-2026-08-05.md`) физически остались, объявленное в самом README правило «применённый файл удаляется» не исполнено ни руками, ни механически. В скоупе `["README.md"]` это было невозможно | Follow-up с расширенным скоупом |
| Текст плана разошёлся с реальностью и не поправлен | План говорит «четыре документа, ждущих руки Президента», факт — 5 файлов, из которых ждут 2. Реализация пошла по живому состоянию (это правильно), но заметка плана осталась `status: draft` без пометки об отклонении, «и следующий читатель поверит плану» | Поправить заметку плана либо пометить в ней отклонение |

**Пропущенных задач в данных нет** — в очереди прогона одна задача; о задачах, пропущенных из-за
провала основания, данных не приходило.