---
title: Аудит трений недели 02–09.08 — как реально шла работа компании агентов
type: note
status: draft
created: 2026-08-09 04:55
updated: 2026-08-09 04:55
permalink: stack/board/audit-nedeli-2026-08-09
tags:
- board
- audit
- friction
---

# Аудит трений недели 02–09.08

Источники: `~/tacticum-vault/00-Board/` (доска, ростер, сигналы, `_workers.md`),
`~/.claude/permission-denied.log` (за неделю 91 реальная запись после отсечки синтетических проб
по методике [[audit-trenie-2026-08-05]]), `~/.claude/autonomy.log`, `~/.claude/logs/tool-failures.log`,
`~/.claude/history.jsonl` (555 сообщений пользователя за неделю), git-история трёх репозиториев.
Только чтение; серверы не трогал. Сверялось с целью: «утром кинул расшифровку → работа идёт сама
и не останавливается → подхожу и вижу готовые сводки».

Сортировка — по цене, самое дорогое сверху.

---

## 1. Мерж — бутылочное горлышко: ветка QA простояла 6 дней и потребовала дня переделки

**ЧТО.** Готовая ветка, ожидая ручного мержа, протухает; сведение с main превращается в
отдельную большую работу, а прежняя верификация обесценивается.

**ДОКАЗАТЕЛЬСТВО.** `_roster.md`, строка lead-qa: ветка `feat/qa-mobile-kit-adoption`
(`efcee066`) доставлена на origin 31.07 10:05 «по прямой команде Президента»; к проверке
06.08 — «**17 конфликтов** с origin/main — ветка стояла, main ушёл на 26 коммитов», мерж
откатывал чужую работу (лейн iva-write-base у двух ролей, PR #227/#230). Сведение заняло
06.08 с ~10:00 (sig-000376 «вливать как есть НЕЛЬЗЯ») до 14:40 (мерж f0c4fe87,
`merge-qa-mobile-kit-2026-08-06` (рабочий vault)), все числа тестов переснимались заново (1298→1319), там же
вскрыто, что прежняя запись «ссылки 8 пар пройдено» была неверной. Найдена коллизия номеров
версий (0.8.0 и 0.4.0 заняты дважды) — прямое следствие того, что ветка и каталог жили
параллельно 6 дней.

**ЦЕНА.** Рабочий день лида 06.08 на сведение и переверификацию + разрешение 17 конфликтов +
коллизия версий в CHANGELOG навсегда. Это самая дорогая единичная переделка недели.

**ЧТО ЧИНИТЬ.** В `~/claude-stack/agent-company.md` (или персону лида): правило «ветка ждёт
мержа больше суток → лид ОБЯЗАН подливать в неё origin/main ежедневно и перегонять гейт»;
ожидание мержа уже не блокирует работу (§Push CLAUDE.md), но за свежесть ветки сейчас не
отвечает никто. Плюс вернуть практику единой PR-очереди (см. п. 3).

## 2. Классификатор-гейт бьёт по легальной работе: 54 блока за неделю, включая чтение и само открытие окна автономии

**ЧТО.** Из 91 отказа гейта за неделю 54 — «Blocked by classifier», и значимая часть из них —
на чтении и на штатных механизмах самого стека. Пик — 42 отказа за день 06.08.

**ДОКАЗАТЕЛЬСТВО.** `~/.claude/permission-denied.log`:
- 2026-08-05 17:31:39 — заблокирован `bash ~/.claude/hooks/autonomy.sh open iva-write 6 --ok
  "Президент в чате…"` — **штатная команда открытия окна автономии с уже полученным ОК**.
  Окно открылось только в 17:42, причём команду в чат набрал сам Президент
  (`history.jsonl` 17:42 `!bash ~/.claude/hooks/autonomy.sh open …`).
- 2026-08-07 12:31:09 и 12:36:00 — заблокирован `git show --stat` (чтение истории рабочего репо).
- 2026-08-07 14:28:51 — заблокирован прогон `pytest` в worktree `bl-670`.
- `measure-deferral-pattern-2026-08-07` (рабочий vault), раздел «НЕ проверено», п. 1 дословно: «свои статусы
  из Taiga снять не смог — **прогон запроса заблокировал гейт разрешений**, поэтому статусы
  взяты из дампа 11:15» — верификатор работал по устаревшим данным.

**ЦЕНА.** Каждый блок — обход, повтор, деградация метода (замер по дампу вместо живых данных);
в случае 05.08 — эскалация до Президента, который сам набирает команду. Это ровно тот «набивной
апрув», против которого писались границы, только теперь его ставит не человек, а классификатор.

**ЧТО ЧИНИТЬ.** (1) Прогнать `/fewer-permission-prompts` / собрать allowlist в
`~/.claude/settings.json` на read-only образцы из журнала: `git show/log/diff` в
`~/tacticum*`, `pytest` в `~/tacticum-worktrees/*`, `python3` в scratchpad. (2)
`autonomy.sh open` — внести в allow как точную форму: команда сама проверяет основание, гейт
поверх неё избыточен. (3) В журнал отказов писать, ЧТО именно не понравилось классификатору —
сейчас «Blocked by classifier» неразбираем постфактум.

## 3. Президент 05.08 два часа работал курьером PR и подгонял вставшего лида

**ЧТО.** Вместо «сказал „делай“ и отошёл» — вечер ручного конвейера: ~10 круговых
«кидай PR → смерджил → что ещё?», плюс дважды пришлось подталкивать остановившегося агента.

**ДОКАЗАТЕЛЬСТВО.** `history.jsonl`, 05.08: 16:16 «Так, почему остановился? Все сделал?»;
16:36 «ты останавливаешься почему-то (ты говоришь: Дальше пишу тесты… и прогоняю всё)» —
агент объявил следующий шаг и встал; 17:24 — **прямая жалоба**: «Доработки по боту нашему
как-то долго уже идут»; далее 17:28, 17:39, 17:41, 17:50, 17:51, 17:54, 17:59, 18:17, 18:29,
19:00 — цепочка «кидай PR / смерджил / есть еще?» по одному PR за раз.

**ЦЕНА.** ~2 часа внимания Президента в режиме диспетчера — самый дорогой ресурс системы; жалоба
на скорость произнесена вслух. Практика единой очереди PR с порядком мержа существовала
(`pr-queue-president` (рабочий vault), 24.07) и к 05.08 не применялась.

**ЧТО ЧИНИТЬ.** В персону лида/ГД: PR отдаются не по одному в чат, а пачкой в одну заметку-очередь
(порядок, зависимости, ссылки) + один сигнал «очередь готова»; правило «объявил следующий шаг —
делай его в этом же ходе, а не жди реплики» (это тот же дефект, что пойман в
`incident-go-plan-mode-2026-07-28`, он вернулся).

## 4. Нет санкционированного пути к ключу Taiga — лид полвечера добывал его грепом по конфигам под градом блоков

**ЧТО.** Для комментариев/статусов в Taiga агенту нужен API-ключ. Ключа нет ни в Keychain, ни в
профиле — лид добывал его регулярками из `~/.claude.json`, классификатор блокировал попытку за
попыткой, знание «где лежит ключ» передаётся сигналами из сессии в сессию.

**ДОКАЗАТЕЛЬСТВО.** `permission-denied.log` 06.08 18:02–23:21 — серия из ~10 блоков lead-backlog
вида `grep -ohE "phk_…" ~/.claude.json`, извлечение из `mcpServers`, `rg` по всему диску
(поиск в 23:13 упёрся в таймаут 2 мин — `tool-failures.log`). sig-000393 (06.08 17:14):
«Taiga прочитана — рабочий ключ phk_535c (лежал у iva-write в ~/.claude.json…)»; назавтра
sig-000406 (07.08 11:59) это же знание пересказывается следующему лиду. Решение «нужен
СЕРВИСНЫЙ ключ, личный admin нельзя» принято 06.08 18:33 (sig-000398), но механики не появилось.

**ЦЕНА.** Часы агентского времени за вечер 06.08; секрет размазан по конфигам и выводам; каждый
новый лид проходит этот квест заново.

**ЧТО ЧИНИТЬ.** Положить сервисный ключ Taiga в Keychain под управлением `secret.sh`
(регламент и так требует «секреты в Keychain, в vault — карта имён»), в профиль
`~/claude-stack/profiles/tacticum.md` — имя ключа и однострочный способ получения. Это снимет и
половину классификаторных блоков п. 2.

## 5. Решение ГД по #816 принято на ложной посылке и отменено через 4 часа

**ЧТО.** Развилка по #816 решена на основании поля `owner` задачи, которое ничего не доказывает;
вечер работы прошёл под неверным решением, ночью — отмена и переделка.

**ДОКАЗАТЕЛЬСТВО.** sig-000399 (06.08 18:49) «РАЗВИЛКА по #816, нужно твоё решение. Критик
доказал числами…» → решение в 18:51 (sig-000400, коммит vault `3b2b64a`) → sig-000402
(22:58): «**ОТМЕНА твоего решения по #816 — посылка была ложной, вина моя**» + коммит
`3738572` «авторство задачи выведено из поля owner, которое ничего не доказывает».

**ЦЕНА.** ~4 часа работы lead-backlog вечером 06.08 под отменённым решением + повторный заход.

**ЧТО ЧИНИТЬ.** В `working-model.md`, шаг развилки: прежде чем ГД решает, критик обязан проверить
не только варианты, но и **посылки** (данные, на которых развилка стоит). Одна строка в регламент —
именно этот класс ошибки за неделю случился в самом дорогом месте: у того, кто решает.

## 6. «Ждёт Президента» размазано по простыням: строка ростера — 5000+ слов, единой очереди решений нет

**ЧТО.** Ростер обещает формат «одна парсируемая строка на лида», а фактически строки lead-qa и
lead-iva-write — многотысячесловные простыни, внутри которых зарыто 10+ пунктов «ЖДЁТ
ПРЕЗИДЕНТА» (мерж, перевыпуск 11 ролей, перевыпуск двух засвеченных OAuth-секретов, 116 битых
ссылок, #510/#713/#146, отсутствие бэкапов прода…). Готовой сводки «что ждёт твоего слова» нет —
её каждый раз собирает разбор дейли.

**ДОКАЗАТЕЛЬСТВО.** `_roster.md`: строка lead-qa (строка 46) и lead-iva-write (строка 44) — по
несколько экранов текста каждая при заявленном в шапке формате
`- lead-<адрес> · <…> · <активно|пауза|закрыто> · <что делает> · [[досье]]`; пункты «ЖДЁТ
ПРЕЗИДЕНТА» стоят в середине абзацев. Находка «на проде НЕТ автоматических бэкапов» — внутри
строки lead-backlog (строка 52), а не отдельным красным пунктом.

**ЦЕНА.** Ежедневное время Президента на выкапывание своих развилок из текста; риск, что дорогой
пункт (бэкапы прода!) просто не будет замечен.

**ЧТО ЧИНИТЬ.** Завести на доске один файл-очередь `zhdet-prezidenta.md` (пункт = одна строка +
ссылка на карточку; лид добавляет, Президент вычёркивает), а в ростере запретить хвост длиннее ~2
предложений — детали уходят в досье. Правка — ростер + персоны лидов.

## 7. Доска и память не коммитились 7 дней в разгар работы

**ЧТО.** «Durable-координация» жила незакоммиченной неделю: между 30.07 01:46 (`918c7f7`) и
06.08 09:49 (`8e59619`) в `~/tacticum-vault` нет ни одного коммита — при том, что 03–05.08 на
доску писались десятки карточек (даты в именах файлов).

**ДОКАЗАТЕЛЬСТВО.** `git -C ~/tacticum-vault log`: соседние коммиты `918c7f7` (07-30) и
`8e59619` (08-06, «память 30.07–06.08: два деплоя, девять PR…» — недельный батч одним разом).

**ЦЕНА.** Всю неделю один сбой диска/ошибочный `checkout` стоил бы недели памяти компании; git
как канал синхронизации и аудита в этот период не работал.

**ЧТО ЧИНИТЬ.** Авто-коммит vault в конец `/wrap` и `/checkpoint` (уже есть свои скрипты — добавить
шаг), плюс проверка в `stack-doctor.sh`: «vault не коммичен > 24ч при изменённых файлах» — жёлтый.

## 8. Роль зарегистрировалась как «--whoami» — сигналы ушли в мёртвый канал, два СРОЧНО-сигнала не прочитаны до сих пор

**ЧТО.** 05.08 lead-iva-write-lane зарегистрировался в реестре под адресом `--whoami` (флаг
утёк в поле имени). Сигналы директора до роли не доходили; слово Президента доставлялось ручным
дублированием.

**ДОКАЗАТЕЛЬСТВО.** `signals/--whoami/inbox/sig-000365.json` (05.08 15:24, «СРОЧНО…
ты слушаешь чужой канал») и `sig-000367.json` (15:36, «ДУБЛИРУЮ слово Президента…») — оба
**по сей день лежат в inbox непрочитанными**; каталог-зомби `signals/--whoami/` существует.

**ЦЕНА.** ~15–30 минут потерянной доставки в горячий момент 05.08 (тот самый вечер п. 3) + ручная
работа директора; канал-зомби остался в дереве и будет ловить будущие опечатки.

**ЧТО ЧИНИТЬ.** В `roles.zsh` при регистрации — валидация адреса (`^lead-|^director$|^stack$`,
запрет ведущих дефисов); в `stack-doctor.sh` — проверка «каталог сигналов не соответствует
ростеру». Каталог `--whoami` разобрать и удалить.

## 9. Журнал воркеров ослеп: 186 «STOP без START» после фикса 05.08

**ЧТО.** Назначение журнала — «START без STOP = воркер не вернулся». Фактически журнал завален
записями `STOP ?xxxxxx … START?` (STOP без парного START, тип воркера не пришёл): 186 в текущем
`_workers.md` рабочего контура + 137 в архиве; шум продолжается и после фикса от 05.08
([[worker-log-fix-2026-08-05]] объяснил старые строки, но новые пишутся 06.08–09.08, включая
сегодняшнюю сессию стека).

**ДОКАЗАТЕЛЬСТВО.** `grep -c "START?" ~/tacticum-vault/00-Board/_workers.md` → 186;
`~/tacticum-vault/91-Archive/workers-2026-08.md` → 137; свежие строки `2026-08-09 03:19 STOP ?99680b …
START?` в `~/stack-vault/00-Board/_workers.md`.

**ЦЕНА.** Диагностика «повис ли воркер» (и `stack-doctor`, который на неё опирается) не работает;
человек, открыв журнал, видит шум вместо линии времени.

**ЧТО ЧИНИТЬ.** В хуке SubagentStart/Stop (`~/.claude/hooks/`): выяснить, почему START теряется
(гонка на `worker-log.lock`? START приходит без имени?) — сейчас чинится следствие (пометки), а не
причина. Это долг стека, в `40-Debt` его строкой нет.

## 10. Воркеры всю неделю спотыкаются об одно и то же окружение: `No module named 'yaml'` — 8 раз у пяти разных агентов

**ЧТО.** Системный python3 без PyYAML; каждый воркер узнаёт это заново падением.

**ДОКАЗАТЕЛЬСТВО.** `~/.claude/logs/tool-failures.log`: 06.08 18:56, 23:12; 07.08 14:08, 14:18,
14:20, 15:03, 15:58, 16:07 — агенты `actrl-75`, `aimpl-81`, `aimpl-62`, `awave1-g`, `acritic-`,
`agate2-c`. Рядом той же природы: `command not found: timeout` (23:28), python3.14 без expat (14:18).

**ЦЕНА.** Минуты на каждое падение × 8 + повторные заходы; мелко, но это чистый повторяющийся налог.

**ЧТО ЧИНИТЬ.** Одна строка в персоны воркеров/профиль: «yaml и прочие пакеты — только через
`.venv` бэкенда (`apps/backend/.venv/bin/python`), системный python3 пуст». Либо один раз
`pip3 install --user pyyaml`.

## 11. Секреты дважды прошли мимо регламента: значение в чате и два OAuth-секрета в выводе диагностики

**ЧТО.** 05.08 Президент передал значение секрета бота прямо сообщением в чат; 06.08 лид
iva-write засветил два OAuth-секрета в выводе своей диагностики — теперь оба ждут перевыпуска.

**ДОКАЗАТЕЛЬСТВО.** `history.jsonl` 05.08 14:57–14:59 («Секрет вставили наш, проверь…» + значение
следующим сообщением; здесь не цитирую). `_roster.md`, строка lead-iva-write: «перевыпуск ДВУХ
OAuth-секретов, **засвеченных мной в выводе диагностики**» — в списке «ЖДЁТ ПРЕЗИДЕНТА».

**ЦЕНА.** Ротация двух секретов (ручная работа Президента) + значения в истории чатов/транскриптах.

**ЧТО ЧИНИТЬ.** Канал приёма секретов: «вставь в Keychain сам: `secret set <имя>` — в чат не
пиши» одной строкой в CLAUDE.md-подсказки; в персоны лидов — правило маскировать значения в любом
выводе (`***` + последние 4 символа).

---

## Что работает и трением НЕ является (чтобы не чинить лишнего)

- **Сигналы доставляются быстро:** 120 сигналов за неделю, медиана «отправлен→увиден» < 1 сек,
  ни одного дольше 30 минут (замер по mtime `seen/`). Канал жив; болит не транспорт, а адресация (п. 8).
- **Механика план→апрув через ГД работает:** sig-000394 «ПЛАН НА АПРУВ» → sig-000395 «АПРУВ ДАН»
  за 2 минуты, Президент делегировал — ровно целевая картина.
- **Гейт ловит и настоящее:** возвраты работы лиду iva-write «ДВАЖДЫ и оба раза по делу»
  (`_roster.md`), блоки прямого ssh в обход ssh-manager — по правилам.

## Чего я НЕ смог проверить и почему

- **Транскрипты сессий поштучно** (`~/.claude/projects/**`, ~1 ГБ) — не прочёсывал; паузы внутри
  сессий («лид объявил план и молчал N минут») мерил только косвенно, по репликам Президента в
  `history.jsonl`. Возможны непойманные простои, о которых он не написал в чат.
- **Ложность каждого из 54 классификаторных блоков** — журнал пишет «Blocked by classifier» без
  причины; поштучный вердикт «ложный/верный» дал только там, где команда очевидно read-only.
- **Чаты с руководителем (зум/тг)** — вне диска; задачи, поставленные там и потерянные до доски,
  не видны.
- **Серверы и Taiga напрямую** — запрещено рамкой аудита (и у аудитора нет ключа — что само по
  себе иллюстрация п. 4).
- **Ночь 05–06.08 в контуре стека** (блоки 23:01–00:40 на локальные `cat`/`echo`) — не отношу к
  трениям: сессия `bcd9aeb3` в это время целенаправленно тестировала гейт (бисекция песочницы),
  отделить пробы от работы по журналу нельзя.
