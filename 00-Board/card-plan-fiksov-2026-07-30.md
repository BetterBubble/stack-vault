---
title: План фиксов по 11 пунктам — 30.07.2026
type: note
status: draft
created: 2026-07-30 00:20
updated: 2026-07-30 00:20
permalink: stack/00-board/card-plan-fiksov-2026-07-30
tags:
- plan
- board
---

# План фиксов по входу Президента

Вход дословно — [[card-dorabotki-po-agentam-2026-07-29]]. Здесь только план и отметки о готовности.
Отметка «сделано» ставится ТОЛЬКО с доказательством: вывод команды, число тестов или дата проверки.

## Порядок и статус

| № | Работа | Файлы | Статус | Доказательство |
|---|---|---|---|---|
| 9a | Чтение серверов без апрувов: `ssh_execute` из `ask` → `allow`, `ssh_db_query` туда же | `settings.json` | **сделано** | `ask` теперь 2 правила, `ssh_db_dump` остался |
| 9b | Гейт различает чтение и запись ПО СОДЕРЖИМОМУ команды, а не по имени инструмента | `guard.sh` | **сделано, после круга слома переписан** | 95/95 проб после починки; ломатель прогнал 99 форм и нашёл 7 классов дыр + 25 ложных отказов — всё закрыто и закреплено |
| 9c | Тесты классификатора закреплены в докторе, чтобы класс не вернулся | `stack-doctor.sh` | **сделано** | 25 кейсов; доктор 255 ok, 0 провалов по тестам |
| 9d | Окно автономии открывает агент — с основанием `--ok`, всё в журнал | `autonomy.sh`, `guard.sh` | **готово к применению рукой** | классификатор не даёт агенту снять правило «только человек»; пакет `~/claude-stack/pending-apply/apply-2026-07-30.sh`, сухой прогон нашёл все 7 фрагментов и проверил синтаксис |
| 0 | Доктор врёт три провала в контуре стека (требует `signals/`, `_roster.md`, project-settings) | `stack-doctor.sh` | **сделано** | в контуре стека сигналы и ростер больше не требуются; третий провал стал точным — роли нет проектных прав |
| 1 | Роли стартуют в auto-mode: и ГД, и лиды | `roles.zsh` | **сделано** | `--permission-mode ${AGENT_PERMISSION_MODE:-auto}` во всех трёх входах, `zsh -n` чист |
| 2 | `13-Deliverables` — то, что читает Президент; архив через 14 дней | `memory-protocol.md`, скрипт, vault | **сделано** | папка + `_index`, `deliverables-sweep.sh` (сухой прогон), подцеплен в `memory-maintenance` |
| 10 | `14-Canon` — дословные материалы, привязанные к фиче/направлению | `memory-protocol.md`, шаблон, vault | **сделано** | папка + `_index` + шаблон `canon-note`; правило «Дословно не редактируется» в §5 регламента |
| 3 | Досье глубже: как работает, как решает боль, что где живёт, откуда взялось | шаблоны, `memory-protocol.md`, доктор | **сделано** | 11 секций у фичи, 9 у направления; доктор проверяет новые варном (старые досье их не имеют) |
| 8 | Версия заметки ПЕРЕПИСЫВАЕТ предыдущую, а не дописывается хвостом | `memory-protocol.md` | **сделано** | §5 + два намеренных исключения (канон, хроника) |
| 5 | Воркер обязан отчитаться; молчание = не сделано, лид не ждёт молча | персоны воркеров и лида | **сделано** | `implementer.md`, `verifier.md`, `lead.md`, `director.md`, чеклист |
| 6 | Не снимать базовые числа через `git stash` при живых агентах — отдельное дерево | ADR + персоны | **сделано** | ADR + правило в `verifier.md`, `implementer.md`, `lead.md` |
| 7 | ГД ревьюит готовое двумя агентами: цель и безопасность прода | персона ГД, агенты | **сделано** | раздел «Приёмка готового» + шаг 2а процесса + ADR |
| 11 | Гейт приёмки: работаем по правильным материалам (репа, созвон, слово разработчика) | `verify-checklist.md`, `gate-check.sh` | **сделано: 2.5 вопроса из 4 механически** | `gate-check.sh --dossier`: сверка `repo:` с `origin`, ссылка на существующий канон с непустым «Дословно», непустая «Как проверяли» — 9/9 проб на поддельных репо и vault. «Тот ли человек» кодом не закрывается и назван в выводе прямо |
| M | Маршрутизация канона и материалов доведена туда, где агент действует | `/after-call`, `/wrap`, `director.md` | **сделано** | правило было только в регламенте, а сводки продолжали ложиться на доску |
| N | Доктор проверяет досье НАПРАВЛЕНИЙ (раньше смотрел только фичи) | `stack-doctor.sh` | **сделано** | 3 провала и 1 варн в рабочем контуре найдены сразу — средний уровень памяти не измерялся никем |
| L | Норма «сервер и main — только после ОК в чате» в персонах и CLAUDE.md | персоны, `CLAUDE.md` | **сделано** | `CLAUDE.md`, `lead.md`, `director.md` |
| 4 | Переложить существующие направления/фичи/проекты | рабочий vault | **не мой контур** | делается из роли `director` после канона |

## Правило захода

Ни один пункт не считается закрытым, пока не проверен воспроизведением. После каждого
содержательного изменения — `stack-doctor.sh`, и результат не хуже предыдущего (228 ok, 1 варн).
В конце — ревизоры на сильных моделях (fable, opus) против готового результата.
