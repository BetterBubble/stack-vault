---
title: Где стоим по работе над стеком
type: note
status: current
created: 2026-07-26 02:14
updated: 2026-08-06 01:45
permalink: stack/sessions/stack-state
tags:
- state
- checkpoint
---

# Где стоим по работе над стеком

**Эта заметка ПЕРЕПИСЫВАЕТСЯ целиком и компактно (≤1 экран), а не дописывается хвостами.**
Хроника — [[stack-chronology-2026-07]]. Долг — [[tech-debt]]. Решения — `20-Decisions/` (20 ADR).
Сводка для Президента за эту ночь — [[utro-2026-08-06]].

## Где стоим (2026-08-06, ночь)

Доктор **466 ok, 0 провалов, 1 варн** (принятый долг). Ночной пайплайн **121 ok** и **впервые
прогнан вживую**. Ссылки чисты в ОБОИХ контурах. Всё запушено в три репозитория.

Заход закрыл: три недовольства Президента, пять его решений, 17 доработок по аудитам, круг слома по
новому разбору, независимую проверку своей же работы и ревизию новой нормы.

## Что сделано за этот заход

- **Окно прода вырезано целиком** — [[adr-window-cut-2026-08-05]]; вместо него «воркер прод не меняет».
- **Две дыры, которых никто не искал:** прямой `ssh` через Bash не проверялся вовсе; запись в код
  через переменную обходила A2. Обе закрыты, обе с тестами.
- **Круг слома 10 (157 форм, 42 дыры в 7 классах)** — разбор переписан с текста на токены.
- **Независимая проверка** (`verifier`) нашла 6 форм из 36, которые я объявил закрытыми: интерпретаторы,
  `flock`/`chronic`, `mosh`/`ansible`. Плюс главное: **тело heredoc разбиралось как команды**, из-за
  чего воркер не мог написать отчёт о ssh-гейте. Всё закрыто.
- **Ревизия новой нормы** (`critic`) нашла 20 пунктов, включая критический: шаблон плана из нормы не
  проходил собственную механику. Закрыто 18, два ушли Президенту.
- **Апрув плана крупной задачи** вернулся по его решению — и держится механикой: `gate-check.sh --plan`,
  ночной пайплайн, шаблон как источник правды, девять пунктов в `verify-checklist`.
- **Воркерам выданы ssh-инструменты чтения**, ревизору — `Write` (без него его разборы не доезжали).
- **Сервер решён:** OVHcloud VPS-4, ключи на VPS — [[adr-perenos-na-vps-ovh]]; покупка за ним.
- **Первый живой прогон пайплайна** — [[adr-suhoj-progon-pajplajna-itogi]]: узел работает, три дефекта.

## Следующий шаг

1. **Его рука, четыре пункта** — все в `~/claude-stack/pending-apply/`: песочница Bash (главное),
   токены MCP, строка в `CLAUDE.md` про апрув, развилка «применяется ли §2а к контуру стека».
2. **Ветка `auto/readme-pending-apply`** — правка README готова, гейт не пропустил из-за недостижимого
   порога в моём же плане. Перегнать с вклад-метрикой, забрать руками или выбросить.
3. **Долг после ночи:** `SSH_RUNNERS` остаётся перечнем, а не правилом (структурный ответ — песочница);
   `secret.sh` под Linux до переезда; `/tidy` на расписание после живого прогона.

## Что заблокировано

Ждёт Президента: покупка сервера · сбор токенов · применение четырёх файлов из `pending-apply`.

## Проверка при холодном старте

```bash
bash ~/.claude/hooks/stack-doctor.sh                    # 466 ok, 0 провалов, 1 варн
bash ~/.claude/hooks/mcp-doctor.sh                      # три сервера сломаны — нужны его токены
bash ~/.claude/hooks/check-links.sh                     # и то же с --vault ~/claude-stack
node ~/.claude/workflows/test-autonomous-feature.js     # 121 ok
python3 ~/stack-vault/90-Materials/probe-ssh-direct-krug10-2026-08-06.py       # 46/46
python3 ~/stack-vault/90-Materials/probe-false-refusals-2026-08-05.py          # 0 живых из 9
```

**Приём, который эта ночь подтвердила трижды:** дыры кончаются там, где проверка перестаёт смотреть на
текст и начинает смотреть на форму. И второй: **тест, который не повторяет способ вызова, защищает не
то** — 118 тестов пайплайна не увидели того, что первый живой запуск нашёл за минуту.
