---
title: Где стоим по работе над стеком
type: note
status: current
created: 2026-07-26 02:14
updated: 2026-08-09 13:14
permalink: stack/sessions/stack-state
tags:
- state
- checkpoint
---

# Где стоим по работе над стеком

**Эта заметка ПЕРЕПИСЫВАЕТСЯ целиком и компактно (≤1 экран), а не дописывается хвостами.**
Хроника — [[stack-chronology-2026-07]]. Долг — [[tech-debt]]. Решения — `20-Decisions/` (21 ADR).
Сводка Президенту — [[utro-2026-08-09]]. План захода — [[card-noch-2026-08-09]].

## Где стоим (2026-08-09, 13:14)

Доктор **552 ok, 0 провалов, 2 варна**. Пайплайн **155 ok**. MCP: живых 10, сломан 0.
Ссылки чисты в обоих контурах. Три репозитория запушены, незакоммиченного нет.
**Очередь на руку Президента пуста** — впервые за сутки.

## Что сделано за заход (ночь 09.08 + утро)

- **Границы прав роли стека** — [[adr-granicy-prav-roli-stek-2026-08-09]]: локально всё, наружу
  четыре личных репозитория, серверы запрещены целиком.
- **Норма плана уточнена и сведена в девяти местах** —
  [[adr-plan-obratimoe-srazu-neobratimoe-posle-razmetki]]: обратимое лид начинает сразу, необратимое
  ждёт разметки ГД, ГД сверяет сначала с созвоном, потом с каноном.
- **Три команды Президента применены:** `CLAUDE.md` приведён к норме · Keychain наполнен (три
  секрета, читаются через `secret.sh get`) · рабочие роли переведены в `bypassPermissions`, границы
  проверены прогоном (SENTINEL, PUSH, A2 держатся — `guard.sh` хук, от режима не зависит).
- **Уведомление про «ждёт решения»**: локальный баннер в `signal-send.sh` + правило `PushNotification`
  в персонах ГД и лида. Только на развилки, не на каждый сигнал.
- **Аудит недели** ([[audit-nedeli-2026-08-09]]) — 11 трений, шесть закрыто.
- **Ресерч чужих стеков** ([[research-chuzhie-steki-2026-08-09]]) — 12 находок, семь из восьми
  рекомендаций внедрены.
- **Транскрибация с узнаванием голосов** ([[transcribe-golosa-2026-08-09]]) — работает, проверена на
  трёх реальных записях.
- **Циклический аудит: 15 кругов, 63 дефекта**, все закрыты.

## Следующий шаг

1. **Шестнадцатый круг аудита** — не запущен (заход прерван чекпойнтом). Круг 15 дал одну находку,
   значит по правилу «два пустых подряд» цикл не закрыт. Задание: проверить починку круга 15
   (классификация автора апрува) + непрочитанные построчно хуки.
2. **Общая сверка норм между файлами** — за сутки семь раз находилось «одно правило в двух
   редакциях». Доктор сверяет только пары шаблонов плана и charter/дефиницию контролёра; остальные
   места не знает. Просится общий список файлов, где живёт норма, и маркерная сверка.
3. **Долг после захода** — в [[tech-debt]]: лок ночного пайплайна · `git` за переменной обходит стоп
   (структурный ответ — песочница) · битые ссылки рабочего vault (работа ГД).

## Что заблокировано

**Песочница Bash** — ждёт обновления CLI (сейчас 2.1.220), ломает статусную строку; конфиг готов в
`pending-apply/sandbox-2026-08-06.md`. **Покупка сервера** — решение принято, отложено Президентом.
**`iva-mcp`** — запись возвращена по его решению, одобрение ждёт выпуска ключа.

## Проверка при холодном старте

```bash
bash ~/.claude/hooks/stack-doctor.sh                    # 552 ok, 0 провалов
bash ~/.claude/hooks/mcp-doctor.sh                      # живых 10, сломанных 0
bash ~/.claude/hooks/check-links.sh                     # и то же с --vault ~/claude-stack
node ~/.claude/workflows/test-autonomous-feature.js     # 155 ok
bash ~/.claude/hooks/pr-queue.sh                        # что ждёт мержа и насколько протухло
```

**Главный урок захода, оплаченный 63 находками.** Правки по находкам сами дали 21% новых дефектов —
но цепочек всего две, обе в текстовом классификаторе стопа, и обе закрыты за три поколения каждая.
Значит цикл закрывает КЛАСС, а не форму, если не останавливаться на первой починке. И второе: за
сутки девять раз врала проба, а не код — тест, не повторяющий форму вызова, обвиняет исправное.
