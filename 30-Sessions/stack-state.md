---
title: Где стоим по работе над стеком
type: note
status: current
created: 2026-07-26 02:14
updated: 2026-08-23 17:37
permalink: stack/sessions/stack-state
tags:
- state
- checkpoint
---

# Где стоим по работе над стеком

**Эта заметка ПЕРЕПИСЫВАЕТСЯ целиком и компактно (≤1 экран), а не дописывается хвостами.**
Хроника — [[stack-chronology-2026-07]]. Долг — [[tech-debt]]. Решения — `20-Decisions/` (26 ADR).

## Где стоим (2026-08-23, 17:37)

**Режим экономии снят сегодня, везде** — [[rezhim-ekonomii-tokenov-2026-08-18]]. Воркеры и
ревизоры обратно на `opus[1m]` (`8aef7ff`), блок режима у лида удалён, маркеры «СНЯТЬ В ПЯТНИЦУ»
убраны (`3abda48`), пять живых лидов уведомлены текстом в окна. Доктор после правок: **549 ok,
0 провалов, 5 варн** — провалов 12.08 больше нет.

**Монитор при этом НЕ возвращён и возвращён быть не должен**, пока ГД закрыт: его выключение
держалось на двух причинах, снята только одна. Причина у строки переписана — снимется вместе с
ГД, а не по календарю.

**Компания живёт на сервере, ГД закрыт Президентом 18.08** — пять лидов работают с ним напрямую:
`lead-qa` · `lead-techwriter` · `lead-onboarding` · `lead-qa-desktop` · `lead-support`.
Управление с мака: `team` (`status` · `new <адрес>` · `restart` · `clean`) и `srv <окно>`.

## Следующий шаг

Открытых обязательств по стеку на сегодня нет — берётся то, что назовёт Президент. Ближайшее
осмысленное из накопленного: **пять дыр в защите** (см. ниже) — единственное, на что «да» уже
сказано, а работа не начата.

## Что открыто

**Пять дыр в защите** (гейт правится через Edit, «read-only» воркеры пишут через Bash,
`tacticum_prod` без режима, heredoc в STACK-PUSH, нет ротации журналов) — Президент сказал «да,
все пять» 18.08, работа не начата.

**Второй том сценариев** — [[Путь задачи изнутри]], круг 1 написан, ждёт правок Президента.
Пишется тем же способом, что первый: три абзаца прозой → правки → итоговый вариант целиком.
Артефакты выделяются ПОСЛЕ утверждения текста.

**Телеграм** — инструкция готова ([[card-telegram-podklyuchenie-2026-08-22]]): две трубы,
скрипты, systemd, риски. Ждёт токена бота. Пульт частично уже работает через Remote Control
(`/rc active` у ролей), телеграм нужен прежде всего ради пушей.

**Два блокера переезда на сервер** из долга — расхождение шаблона новой машины с живым конфигом
по `skipDangerousModePermissionPrompt` и проверка «дублей мониторов», выдающая мусор вместо имён.
Оба помечены «чинить первым шагом переезда».

**Сервер отстал от мака** по `~/claude-stack`: другой HEAD и четыре файла с незакоммиченными
правками (`roles.zsh`, `roles/lead.md`, `roles/director.md`, `profiles/tacticum.md`). Расхождение
замечено попутно, в работу не бралось.

## Что заблокировано

**Токен телеграм-бота** и **покупка домена** — действия Президента. **Доставка правок конфига и
персон на сервер** — тоже: гейт CONFIG не пускает запись через Bash, файлы кладутся в `/tmp`,
Президент выполняет `cp`.

**Три вопроса из записки режима** ждут его решения: нужен ли монитор при закрытом ГД · вносить ли
перезапуск между задачами в канон навсегда · лимит на документы.

## Чего делать НЕ надо

**Не вооружать монитор**, пока ГД закрыт — будить лида некому.
**Не урезать окно контекста ролей до 200k** — отвергнуто 18.08: лидам нужен длинный контекст.
Урок: экономия, отнимающая у роли рабочий инструмент, не проходит.
**Не гонять развилки из раскопок 17.08 списком** — Президент назвал это кашей; работа идёт от
сценария, а не от списка находок.

## Урок захода

**Слепая вставка текста в чужое окно затирает то, что там уже набрано.** Рассылая снятие режима
пяти лидам, отправил текст в окно `lead-support`, где в промте лежал недописанный вопрос
Президента, — моё сообщение приклеилось к нему хвостом. Чистка `BSpace` по числу символов съела
и его текст тоже (восстановил дословно из `capture-pane`). **Правильный порядок — сначала снять
`capture-pane` и посмотреть, пуст ли промт, и только потом слать.** Проверял занятость окон, но
по индикатору хода, а не по содержимому поля ввода: окно «не работает» и «в него ничего не
набрано» — разные вещи.
