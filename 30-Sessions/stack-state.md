---
title: Где стоим по работе над стеком
type: note
status: current
created: 2026-07-26 02:14
updated: 2026-08-05 23:15
permalink: stack/sessions/stack-state
tags:
- state
- checkpoint
---

# Где стоим по работе над стеком

**Эта заметка ПЕРЕПИСЫВАЕТСЯ целиком и компактно (≤1 экран), а не дописывается хвостами.**
Хроника — [[stack-chronology-2026-07]]. Долг — [[tech-debt]]. Решения — `20-Decisions/` (18 ADR).

## Где стоим (2026-08-05, ночь)

Заход по трём недовольствам Президента и пяти его решениям **закрыт**; 17 доработок по двум аудитам
разобраны и **отвечены им поштучно** — карточка [[card-17-dorabotok-2026-08-05]]. Доктор
**417 ok, 0 провалов**, ночной пайплайн 115 ok, ссылки чистые, всё запушено.

## Что сделано за этот заход

- **Окно прода вырезано целиком** — [[adr-window-cut-2026-08-05]]. Вместо него: воркер прод не
  меняет, две ручные кнопки стопа, норма в CLAUDE.md. Заодно доктор перестал врать: 163 проверки
  классификатора шли по настоящему `$HOME` и печатали 105 провалов на исправном коде, когда рабочий
  контур деплоил.
- **Найдена и закрыта дыра, которой никто не искал:** прямой `ssh` через Bash не проверялся вовсе —
  `ssh root@prod 'rm -rf /srv/app'` проходил молча, потому что 673 формы разбора применялись только к
  MCP-инструментам. Теперь удалённая команда идёт через тот же классификатор; 15 тестов.
- **Вторая дыра того же класса:** `echo z > $AGENT_CODE_ROOT/x.py` из роли `director` проходил мимо
  A2 — цель с переменной склеивалась с рабочим каталогом. Закрыто, 3 теста.
- **Воркерам выданы ssh-инструменты чтения** — [[adr-workers-read-prod-lead-delegates]]. Их не было
  ни у одного, поэтому лид лазил на сервер сам: причина была механическая, а не дисциплинарная.
- **Блок PR держится хуком** (32 просьбы «кидай PR с описанием», дважды «снова»), `/tidy` доходит до
  лида, `/after-call` ловит его язык, журнал отказов разделён с тестовым.
- **Сервер решён и записан** — [[adr-perenos-na-vps-ovh]]: OVHcloud VPS-4, ключи на VPS, Syncthing на
  одну папку, `remoteControlAtStartup: true` уже включён.

## Следующий шаг

1. **Вставить 4 строки в `allow`** — `~/claude-stack/pending-apply/allow-rules-2026-08-05.md`. Мне
   классификатор харнесса это запрещает (расширение своих прав). После вставки — синхронизировать
   `settings.example.json`, иначе доктор честно ругнётся на расхождение с шаблоном.
2. **Формат вердикта внести в канон** `agent-company.md` (неприкосновенный, только вместе с ним):
   сейчас он живёт в пяти персонах, а источника правды у копий нет.
3. Разобрать отчёты двух тиммейтов (MCP-доктор, журнал воркеров) — в работе на момент записи.
4. Первый сухой прогон ночного пайплайна вживую (115 тестов есть, живого запуска нет).

## Что заблокировано

Ждёт Президента: покупка сервера («вернёмся как-нибудь») · сбор токенов («потом») · напоминание об
истечении логина — решено НЕ делать, вернуться при переезде на сервер.

## Проверка при холодном старте

```bash
bash ~/.claude/hooks/stack-doctor.sh                          # 417 ok, 0 провалов, 1 варн (долг)
bash ~/.claude/hooks/mcp-doctor.sh                            # НОВОЕ: что с MCP-серверами
bash ~/.claude/hooks/check-links.sh                           # память стека: битых 0
node ~/.claude/workflows/test-autonomous-feature.js           # 115 ok
python3 ~/stack-vault/90-Materials/probe-false-refusals-2026-08-05.py   # ложные отказы: 0 живых из 9
```

**Приём, который стоит помнить:** аудит журнала отказов описывает ПРОШЛОЕ. Из девяти ложных отказов,
которые он нашёл, к вечеру не воспроизводился ни один — часть закрылась июльскими правками, один
закрылся сам вместе с вырезанным окном. Перед правкой по такому списку первым шагом идёт проба на
живом коде, а не правка.
