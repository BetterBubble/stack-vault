---
title: Где стоим по работе над стеком
type: note
status: current
created: 2026-07-26 02:14
updated: 2026-08-09 03:50
permalink: stack/sessions/stack-state
tags:
- state
- checkpoint
---

# Где стоим по работе над стеком

**Эта заметка ПЕРЕПИСЫВАЕТСЯ целиком и компактно (≤1 экран), а не дописывается хвостами.**
Хроника — [[stack-chronology-2026-07]]. Долг — [[tech-debt]]. Решения — `20-Decisions/` (21 ADR).

## Где стоим (2026-08-09, ночь)

Доктор **467 ok, 0 провалов, 1 варн** (принятый долг — 50 worktree). MCP: **живых 9, сломан 1**.
Заход короткий: закрыты две вещи, которые ждали Президента с 06.08, и одна развилка нормы.

## Что сделано за этот заход

- **Токены MCP доехали.** Президент заменил токен в одном файле из трёх; в `~/tacticum/.mcp.json` и
  `~/tacticum-worktrees/.mcp.json` оставался старый — трекер и вики не работали трое суток. Раскатано,
  бэкапы `.bak-2026-08-09`. Плюс вторая половина поломки: у `taiga` сменили url на `/mcp`, а `type`
  остался `sse` — с таким сочетанием сервер не поднимется и с верным токеном. Исправлено в трёх файлах.
  Проверял **прямыми запросами по каждому файлу**, а не доктором: доктор смотрит один каталог.
- **Развилка §2а закрыта решением Президента** — [[adr-kontur-steka-bez-plana-i-apruva]]: контур стека
  исключение целиком, ни плана крупной задачи, ни апрува; работает объявление намерения по §2.
  `working-model.md` §2а получил пункт 4, `roles/stack.md` не тронут — он говорил правильно.
- **Ссылка на VPS-4 отдана**, цена сверена на живой странице (€23.75/мес при годовой предоплате).

## Следующий шаг

1. **`basic-memory` ломает шапку новой заметки** — два frontmatter-блока, снятие не помогает (три
   попытки, три возврата). Заденет каждую новую заметку в обоих контурах. Разбирать отдельным заходом.
2. **`iva-mcp`** — 404 на любом токене, сервис, похоже, переехал на `helm.tacticum.ru`. Нужен верный
   URL либо удаление записи.
3. **`dossier-archive.sh`** падает при каждом старте здесь: ищет `12-Features` рабочего vault.
4. Долг с прошлого захода: `SSH_RUNNERS` перечнем вместо правила · `secret.sh` под Linux до переезда ·
   `/tidy` не на расписании и ни разу не запускался.

## Что заблокировано

Ждёт Президента: **покупка сервера** (решение принято, отложена им же) · **песочница Bash** — ждём
обновления CLI, сейчас всё та же 2.1.220, конфиг готов в `pending-apply/sandbox-2026-08-06.md`.

## Проверка при холодном старте

```bash
bash ~/.claude/hooks/stack-doctor.sh                    # 467 ok, 0 провалов, 1 варн
bash ~/.claude/hooks/mcp-doctor.sh                      # живых 9, сломан 1 (iva-mcp, 404 по адресу)
bash ~/.claude/hooks/check-links.sh                     # и то же с --vault ~/claude-stack
node ~/.claude/workflows/test-autonomous-feature.js     # 121 ok
python3 ~/stack-vault/90-Materials/probe-ssh-direct-krug10-2026-08-06.py       # 46/46
python3 ~/stack-vault/90-Materials/probe-false-refusals-2026-08-05.py          # 0 живых из 9
```

**Что подтвердил этот заход.** «Починено» и «проверено» — разные слова: замена токена в одном файле
из трёх выглядела как починка ровно до прямой пробы каждого файла. И второе: доктор проверяет тот
путь, который знает, — если запись живёт в трёх местах, зелёный доктор не значит рабочую систему.
