---
title: Шаблон — план крупной задачи
type: reference
status: current
created: 2026-08-06 01:35
updated: 2026-08-06 01:35
permalink: stack/templates/plan-note
tags:
- template
- plan
- process
---

# Шаблон — план крупной задачи

**Это источник правды по ФОРМАТУ плана.** Формулировки ниже дословно совпадают с тем, что читает
механика (`gate-check.sh --plan`, `planProblems` в ночном пайплайне). Меняешь формат здесь — меняй и
разбор, иначе получится ровно то, что случилось 06.08: норма задавала одну форму, гейт искал другую,
и первая крупная задача по новой норме встала бы с сообщением «раздела нет».

Норма, зачем это нужно и кто апрувит — `~/claude-stack/working-model.md` §2а. Здесь только форма.

**Когда нужен:** только для КРУПНОЙ задачи (>10 файлов или >150 строк, либо любая необратимость:
прод, БД, чужие репозитории, деньги, либо фича целиком с PR). У лёгких и средних плана нет вовсе —
там сверка постановки без ожидания ответа.

**Работа под план не встаёт.** Решение Президента 09.08: план предъявляется и размечается ГД
асинхронно (апрув · не апрув · на переделку · решай сам · не понятно), а лид сразу берётся за
обратимую часть. Останов остался ровно в двух местах: деплой на прод и мерж в `main`.

**Копия этого файла живёт в рабочем vault** (`~/tacticum-vault/92-Templates/plan-note.md`) — дома
памяти не смешиваются, поэтому копий две. **Правятся они вместе**, и это не пожелание: пятый круг
аудита 09.08 нашёл их разошедшимися через полтора часа после создания второй — четвёртый случай
«двух редакций одной нормы» за сутки. Сверку пары держит `stack-doctor.sh`.

---

## Что механика требует от плана — пять вещей

Ни одна из них не «для порядка»: у каждой есть потребитель в коде.

| Что | Форма | Кто читает |
|---|---|---|
| строка апрува | `апрув: <кто>, <когда>` | `gate-check.sh --plan`, ночной пайплайн |
| границы | `## Что НЕ трогаем` **либо** строка `Что НЕ трогаем: …` | там же |
| скоуп | `scope:` и список путей/глобов | `gate-check.sh --scope` (разрастание объёма) |
| приёмка | `acceptance:` и числовые пороги вида `метрика >= 0.62` | `gate-check.sh --metrics` |
| флаг автонома | `autonomy: on` — **только** если задача идёт в ночной прогон | `planProblems` пайплайна |

---

## Сам шаблон (копируй целиком)

```markdown
---
title: План — <тема>
type: note
status: current
created: <ГГГГ-ММ-ДД ЧЧ:ММ>
updated: <ГГГГ-ММ-ДД ЧЧ:ММ>
permalink: <namespace>/board/plan-<тема>
tags:
- board
- plan
---

# План: <тема>

апрув: Президент, <дата> в чате
autonomy: on        <!-- только для ночного прогона; днём строку не ставить -->

## Что сделаем
<по существу, без внутренних терминов — это читает человек за минуту>

## Как это будет выглядеть
<что он увидит: экран, поведение, число, строка в логе>

## Что НЕ трогаем
<перечислить прямо: соседние фичи, файлы, схемы, окружения. САМЫЙ ВАЖНЫЙ раздел —
 именно им Президент проверяет план, и именно его контролёр сверяет с фактическим диффом>

scope:
  - services/<фича>/**
  - tests/<фича>/**

acceptance:
  - recall@10 >= 0.62
  - p95_ms <= 900

## Приёмка словами
<чем поймём, что готово, если числа не всё покрывают: живая проверка, экран, поведение>
```

---

## Три правила, которые к формату не относятся, но без них план не работает

1. **План живёт заметкой на доске, а не в чате.** Слово, сказанное в чат и оставшееся в чате, не
   переживает `/checkpoint`, не даёт цели для `--plan` и исчезает при перезапуске сессии. Поэтому
   строка `апрув:` пишется В ЗАМЕТКУ сразу, а не «потом оформим».
2. **Разметка действительна для ТОЙ версии плана, которую видели.** Изменил «Что НЕ трогаем», скоуп
   или приёмку — прежняя разметка аннулирована, план предъявляется заново. Иначе строка остаётся
   зелёной, а границы под ней уже другие: формально проверка пройдена, фактически их никто не видел.
3. **Отозванная разметка = стоп реализации.** Президент передумал — работа не «доводится по
   инерции»: незакрытое уходит в очередь решений, ветка остаётся как есть, факт отзыва пишется в
   план. Отзыв — активное действие; молчание работу не останавливает никогда.

## Приёмка должна быть достижима внутри скоупа

Найдено первым живым прогоном ночного пайплайна (06.08): порог `битых_ссылок <= 0` при скоупе
`README.md` недостижим, потому что битые ссылки лежали в других файлах. Гейт заблокировал исправную
работу, и был прав.

**Метрика состояния** («во всём vault ноль битых ссылок») в задаче на один файл — это требование
починить чужое, а не приёмка. Правильная форма — **вклад-метрика**: «новых битых ссылок 0»,
«регрессии нет: было 3 → стало 3».
